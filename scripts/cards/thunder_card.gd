extends BaseCard
class_name ThunderCard

func use(world: World , cell: Vector2i):
	pass
	#var board = world.board
	#var diff = board.get_map_center() - cell
	#var dir = Vector2i(
		#sign(diff.x),
		#sign(diff.y)
	#)
	#for plant in world.get_all_plants():
		#plant.on_wind(dir)
